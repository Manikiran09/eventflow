# For same-domain deployments behind reverse proxy, keep /api
VITE_API_URL=/api

# Backend public URL for loading uploaded images from /uploads
# Example: https://api.your-domain.com
VITE_BACKEND_URL=http://localhost:5000
